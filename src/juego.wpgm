import wollok.game.*
import hud.*
import jugador.*
import entorno.*
import enemigos.*
import movimientoEntidades.*
import mapa.*
import generadorDeEnemigos.*

program juego {
	game.title("Dungeon zx")
	game.ground("pasto-0.png")
	mapa.generar() // NO TOCAR
	
	generadorDeEnemigos.generar()
	hud.add()
	game.allVisuals().forEach({ element => element.comportamiento() })
	game.start()
	
}

